# Development / test dependencies for PyArco.
#
# Install into the project venv:
#     .venv/bin/pip install -r requirements-dev.txt
#
# Run the offline test suite:
#     .venv/bin/python -m pytest python25/tests -v
pytest>=8.0
